{
  "name": "barkapi",
  "version": "0.1.0",
  "description": "API Contract Drift Detector — Your API's watchdog",
  "keywords": ["api", "openapi", "swagger", "drift", "schema", "contract", "testing"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/barkapi/barkapi.git"
  },
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "barkapi": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": ["dist/", "README.md"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "build:publish": "node scripts/build-publish.js",
    "prepublishOnly": "npm run build:publish"
  },
  "dependencies": {
    "@apidevtools/swagger-parser": "^10.1.0",
    "better-sqlite3": "^11.0.0",
    "chalk": "^4.1.2",
    "commander": "^12.0.0",
    "next": "14.2.35",
    "ora": "^5.4.1",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@barkapi/core": "0.1.0",
    "typescript": "^5.4.0"
  }
}
